name: Generate Markdown

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  generate-markdown:
    name: Generate Markdown
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Print Markdown file
        run: mvn -q -e -DskipTests exec:java